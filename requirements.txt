# Thư viện chạy ứng dụng. Khoảng phiên bản mở để cài được trên Python 3.11 và 3.12.
# Đã kiểm thử ngày 18-09-2026 với Python 3.11.15 (macOS arm64) và các phiên bản:
#   google-api-python-client 2.200.0, python-dotenv 1.2.3, pandas 2.3.3, numpy 2.4.6, pyvi 0.1.1,
#   bertopic 0.17.4, torch 2.14.0, sentence-transformers 6.0.1, umap-learn 0.5.12, hdbscan 0.8.44,
#   streamlit 1.64.0, openai 3.14.1, scikit-learn 1.9.1, joblib 1.6.0, plotly 7.1.0,
#   matplotlib 3.11.2, seaborn 0.13.2.
# Python 3.13 chưa kiểm thử (wheel của hdbscan/umap-learn/torch thường ra chậm hơn).

# Thu thập dữ liệu
google-api-python-client>=2.108,<3
python-dotenv>=1.0,<2

# Xử lý dữ liệu và tiếng Việt
pandas>=2.1,<3
numpy>=1.26,<3
pyvi==0.1.1

# Mô hình chủ đề
bertopic>=0.16,<1
torch>=2.2
sentence-transformers>=2.7,<7
umap-learn>=0.5.5,<0.6
hdbscan>=0.8.33,<0.9

# Mô hình cảm xúc
scikit-learn>=1.4,<2
joblib>=1.3,<2

# Giao diện, biểu đồ, LLM cục bộ (Ollama qua API tương thích OpenAI)
streamlit>=1.30,<2
plotly>=5.18,<8
openai>=1.10,<4
matplotlib>=3.8,<4
seaborn>=0.13,<0.14

[pytest]
testpaths = tests
pythonpath = src
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
markers =
    slow: kiểm thử chạy lâu (khởi động Streamlit, mô hình)
